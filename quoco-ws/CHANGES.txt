Mainly followed instructions from the PDFs.
Used code snippets from slides provided on moodle.
Used some of the same code samples seen on demonstration videos.
Added print statments for code check (ex: linkedlist)
Added jmDNS.
Reformatted code.

To run the project:
- mvn clean install
- mvn compile 
- docker-compose build --no-cache 
- docker-compose up

Open new terminal
- docker-compose build client 